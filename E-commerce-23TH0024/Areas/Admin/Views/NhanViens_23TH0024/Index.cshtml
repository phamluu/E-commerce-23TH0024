@* @model IEnumerable<E_commerce_23TH0024.Models.NhanVien>

@{
    ViewBag.Title = "Quản lý nhân viên";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
    var grid = new WebGrid(Model, canPage: true, defaultSort: "UserID", canSort: true, rowsPerPage: 10);
}

<h4 class="title">Quản lý nhân viên</h4>

<p>
    @Html.ActionLink("Thêm mới", "Create", "", new {@class = "btn btn-add"})
</p>


<div id="gridContent">
    @ViewBag.TB
    @grid.GetHtml(
    tableStyle: "table table-borderd table-hover",
    headerStyle: "grid-header",
    footerStyle: "grid-footer",
    fillEmptyRows: false,
    mode: WebGridPagerModes.Numeric,
    alternatingRowStyle: "alternatingRowStyle",
    columns: new[]
    {
         grid.Column("STT",format: item => item.WebGrid.Rows.IndexOf(item) + 1 + Math.Round(Convert.ToDouble(grid.TotalRowCount / grid.PageCount) / grid.RowsPerPage) * grid.RowsPerPage * grid.PageIndex),
         grid.Column("HoTen","Họ tên"),
         grid.Column("User.UserName","Tên đăng nhập"),
         grid.Column("SoDienThoai","Số điện thoại"),
         grid.Column("DiaChi","Địa chỉ"),
         grid.Column("Chức năng", format: @<text>
    <a href="@Url.Action("Edit", new { id = item.MaNV})" class="edit-btn"><abbr title="Cập nhật"><i class="fa fa-edit"></i></abbr></a>
    <a href="@Url.Action("Details",new { id = item.MaNV})" class="detail-btn"><abbr title="Xem chi tiết"><i class="fa fa-eye"></i></abbr></a>
    <a href="@Url.Action("Delete", new { id = item.MaNV})" class="delete-btn"><abbr title="Xóa dữ liệu"><i class="fa fa-trash"></i></abbr> </a>
        </text>),
})
</div>



 *@