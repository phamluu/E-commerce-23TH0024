@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-actions mt-3 mb-4">
        <input type="submit" value="Xóa" class="btn btn-default" /> |
             @Html.ActionLink(
            linkText: "Quay lại danh sách",
            actionName: "Index",
            routeValues: null,
            htmlAttributes: new { @class = "btn btn-default btn-list btn-primary" }
        )
    </div>
}