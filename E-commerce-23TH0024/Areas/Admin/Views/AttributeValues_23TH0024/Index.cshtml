@using E_commerce_23TH0024.Models.Ecommerce
@model IEnumerable<AttributeValue>

@{
    ViewBag.Title = "Giá trị thuộc tính";
}

<style>
    .table th, .table td {
        font-size: 0.95rem;
        vertical-align: middle;
    }

    .table td {
        white-space: normal;
    }

    .btn-sm i {
        font-size: 1rem;
    }
</style>

<section class="mb-4 mt-4">
    <div class="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-3">
        <h2 class="fs-5 fw-semibold text-primary d-flex align-items-center gap-2 mb-0">
            <i class="bi bi-card-text"></i> @ViewBag.Title
        </h2>
        <a href="@Url.Action("Create")" class="btn btn-sm btn-primary d-flex align-items-center gap-1">
            <i class="bi bi-plus-circle"></i> Thêm mới
        </a>
    </div>

    <partial name="_AlertMessages" />

    <div class="card shadow-sm border border-light-subtle rounded-3">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover table-striped table-bordered align-middle mb-0">
                    <thead class="table-light text-nowrap">
                        <tr>
                            <th>Giá trị</th>
                            <th>Thuộc tính</th>
                            <th class="text-center">Chức năng</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model != null && Model.Any())
                        {
                            foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.Value</td>
                                    <td>@(item.ProductAttribute?.AttributeName ?? "Không xác định")</td>
                                    <td class="text-center">
                                        <partial name="_TableActions" model="item.Id" />
                                    </td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="3" class="text-center text-muted">Không có dữ liệu</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</section>
